@model IEnumerable<Web.Models.ProjectCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProjectCategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProjectCategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProjectCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProjectCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProjectCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
